# Pinned to the 0.3 line, where AgentExecutor lives in `langchain.agents`.
# (agent.py also falls back to `langchain_classic` for LangChain 1.x.)
langchain>=0.3.20,<0.4
langchain-core>=0.3.60,<0.4
langchain-openai>=0.3.0,<0.4
python-dotenv>=1.0
pytest>=8.0
