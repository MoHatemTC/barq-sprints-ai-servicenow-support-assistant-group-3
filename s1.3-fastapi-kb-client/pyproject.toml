[project]
name = "s1-3-fastapi-kb-client"
version = "0.1.0"
description = "FastAPI service that retrieves published ServiceNow KB articles via OAuth"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic-settings",
]